defaults:
  - algorithm: ppo
  - env: mjx_dmc
  - networks: ppo
  - logging: wandb_offline
  - runner: gymnax
  - _self_
  
seed: 0
name: "ppo"
tags: ["ppo", "mujoco_playground"]
hydra:
  job:
    chdir: True
  searchpath:
    - file://config
