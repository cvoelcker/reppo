defaults:
  - algorithm: dpo
  - env: brax
  - networks: ppo
  - logging: wandb_offline
  - runner: gymnax
  - _self_
  
seed: 0
name: "dpo"
tags: ["dpo", "brax"]
hydra:
  job:
    chdir: True
  searchpath:
    - file://config
