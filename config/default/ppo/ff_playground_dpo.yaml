defaults:
  - algorithm: dpo
  - env: mjx_dmc
  - networks: ppo
  - logging: wandb_offline
  - runner: gymnax
  - _self_
  
seed: 0
name: "dpo"
tags: ["dpo", "mujoco_playground"]
hydra:
  job:
    chdir: True
  searchpath:
    - file://config
